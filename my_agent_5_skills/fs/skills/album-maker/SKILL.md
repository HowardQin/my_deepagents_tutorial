---
name: album-maker
description: 根据用户的描述，生成图片和相册
---
# 1. 创建一个目录
根据相册的主题，在根目录下创建一个目录。

# 2. 根据用户的描述生成图片
在前一步的目录下，根据用户的描述生成图片，如果生成多张图片则用并行任务生成。

# 3. 生成HTML相册
在前一步的目录下，生成相册index.html，包含目录下的所有图片，图片的路径改为相对于index.html的路径。
